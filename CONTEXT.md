# MeetPR iOS 词汇表

学员端 + 教练端 SwiftUI 应用。本文件只收 MeetPR 特有概念的正典叫法，禁实现细节。

## Language

**游标日（cursor day）**:
当前计划内按序第一个未完成的训练日；学员的「下一次训练」永远是它。推进制（spec 071）的核心。
_Avoid_: 今日训练日、当天计划

**推荐日期（recommended date）**:
训练日在日历上给学员看的参考日期（「教练推荐 9月8日」）：教练排期的位置投影，叠加教练最新一次「后移」。纯展示，不改变游标。
_Avoid_: 原定日期、计划日期、scheduled date（代码里位置投影的字段名，不是正典用语）

**后移（plan shift）**:
教练在网页端把某日起未完成训练日的推荐日期整体延后 N 天的动作；学员端只消费结果（推荐日期变化 + 一条推送），不提供入口。
_Avoid_: 顺延（V1/V2 学员自助功能名，spec 071 已下线）、推迟、延期
