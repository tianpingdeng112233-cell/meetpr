MeetPR 卡片：暗色 #141416 圆角 16 无投影（层级靠背景深浅），亮色白卡 + `0 4px 18px rgba(17,24,39,.06)`。焦点卡加 `accent`（左 3px 金条）。卡内嵌套用 `inset`。

```jsx
<Card accent>
  <div style={{fontSize:13,fontWeight:700}}>教练反馈</div>
  <div style={{fontSize:14,color:'#B8B8BE'}}>深蹲第一组速度很好…</div>
</Card>
```
