组次记录表。行内数据全部 IBM Plex Mono；已记录行白/墨字，未记录降灰。右侧两列图标：成败（✓/✗/空圈）+ 视频上传状态（摄像机绿/灰）— 两者语义独立。

```jsx
<SetTable rows={[
  {w:175, r:3, rpe:8.5, done:true, video:true},
  {w:175, r:3, rpe:8.5, done:true, failed:true, video:true},
  {w:175, r:3, rpe:8.5}
]} />
```
