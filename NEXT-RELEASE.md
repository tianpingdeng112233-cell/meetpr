# 下个 TestFlight 版本 — 进行中

> 本文件在 `release/1.0` 分支上,记录**下一个内测包**的内容与进度。
> 发版模型:1.0(7) 基底小步直推,落线即候选,tag 切包;main = 大包待分诊库存,不进本线。
> 权威规则见 [AGENTS.md §发版直推流](./AGENTS.md)。

## 🎯 目标:1.0 (10)
- 基底:`beta/1.0-9`(= TestFlight 在测的 1.0(9),2026-07-12 上传)之后的 release/1.0 直推累积
- build 号:10(prep-beta 时统一 bump,平时不动)
- 切包:本分支打 tag `beta/1.0-10`

## 本版将包含(落线后追加到这里)

- e1RM 比赛主项按学员 onboarding 解析，并一次性重算端上存量历史、重置旧 PR 基线（port of #250，spec 050）。
- 视频选片即出「准备中…」占位(spec 027,#249):相册/相机选中瞬间显示 spinner,根治「点了卡 3-5s 零反馈」;前身 #248 曾随 1.0(9) revert 撤出,本班车正式上车。⚠️ 3-5s 时序模拟器复现不了,随本包 TestFlight 真机亲验。
- 学员端休息计时器可见性(spec 055,#243):首次触发弹一次性说明卡(RPE 分档规则+指路设置);「我的」新增「组间休息设置」(自动按 RPE / 固定时长 30-600s);回落链=教练逐组处方 > 学员固定偏好 > RPE 自动表,学员偏好永不覆盖教练处方。⚠️ 实装轮未取到运行态截图,说明卡/设置页视觉随本包或走查亲验。
- 学员端记录组弹窗新增「上赛扣」勾选(#256):勾上则 2.5kg 赛扣计入配重(每边少摆 2.5kg,图示+明细联动),默认不勾=纯盘子;选择跨组跨启动记忆。
- 学员端「输入教练邀请码」页姓名栏文案消歧(标签「你的姓名」+占位「填你自己的名字」+辅助行「教练会在学员列表里看到这个名字」),杜绝学员把自己的姓名栏误填成教练名字。
- 学员端记录组弹窗:重量/次数/RPE 数字改为填充输入框样式(点整块 slot 聚焦弹键盘),解决学员不知道数字可点击直接输入只会用 ± 号;含输入长度上限防溢出 + VoiceOver 标签(#253)。
- 内测封存评估期入口(#254,David 07-13 拍板):学员端 accepted 一律直进 5 tab(存量未完成评估期不再锁人),教练端接收弹窗去掉评估期二选一、恒定跳过;评估期代码原地休眠,defer≠delete。
- 加入自建一等公民埋点(spec 043):21 个行为事件、离线队列/崩溃补报/远程熄火、登录前一次性隐私告知与可选卡点反馈;Demo 构建硬禁用,不产生埋点数据。⚠️ **真 gate 待拍**:带埋点的包上 TestFlight 前,David 必须敲定 PrivacyInfo/PIPL 隐私文案 + ASC 隐私问卷三类勾选(与 PrivacyInfo.xcprivacy 三 dict 一一对应),口径未定前**别圈进 beta**。

## 📋 进度:离 archive 还差几步
- [x] ~~解禁可合的上班车遗留~~:#249 视频占位、#243 spec 055 休息计时器已合(2026-07-13,连同
      #257 埋点、#256 上赛扣共四张;#243/#256 与线上 spec 050/#253/#257 的冲突已解,StudentKit
      340 测试绿 + 全 app 模拟器构建验证)
- [ ] 捞回队列余项逐个落线:#227 导入历史 e1RM 回填(依赖的 #216/#220 已随 1.0(9) 发出,
      **已解锁**,体量大拆子卡)/ per-set 逐组目标 / rename-student / #215'/#217' 裁剪卡(等 Claude)/
      #234a 非 UI 拆包(等 Claude),状态见 [AGENTS.md §发版直推流](./AGENTS.md) 捞回队列表
- [ ] fadbc3a 的 review-loop 补审(1.0(9) 遗留,先发后补审);#256 同欠一道 Codex 互审
      (实装 session codex 起不动,合并前 Claude 已亲审全量 diff,建议随 fadbc3a 一起补)
- [ ] build 号 bump 10(prep-beta,agvtool 单源)+ 打 tag `beta/1.0-10`
- [ ] David:Archive → Upload → 分配 Neice

## 收尾约定
archive 上传后:把「本版将包含」挪进 RELEASES.md 作 1.0(10) 一节,清空本文件、目标号 +1。
